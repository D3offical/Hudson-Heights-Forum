const b=document.getElementById('modalBackdrop'),c=document.getElementById('modalContent'),m=document.getElementById('mainNav');const mb=document.getElementById('mobileMenu');if(mb)mb.onclick=()=>m.classList.toggle('show');function openM(h){c.innerHTML=h;b.classList.add('show')}document.getElementById('modalClose').onclick=()=>b.classList.remove('show');b.onclick=e=>{if(e.target===b)b.classList.remove('show')};document.querySelectorAll('[data-modal]').forEach(x=>x.onclick=()=>openM(`<h2>${x.dataset.modal==='register'?'Create Account':'Log in'}</h2><p>Frontend preview. Connect Supabase later for real accounts.</p><div class="field"><label>Username</label><input></div><div class="field"><label>Password</label><input type="password"></div><button class="btn btn-primary full" onclick="alert('Backend coming next')">Continue</button>`));const f=document.getElementById('factionForm');if(f)f.onsubmit=e=>{e.preventDefault();openM('<h2>Application Ready</h2><div class="notice">The form works visually. Supabase is needed to save submissions and let staff review them.</div>')};document.querySelectorAll('[data-faction]').forEach(x=>x.onclick=()=>openM(`<h2>${x.dataset.faction}</h2><div class="notice">Faction profile pages can show roster, turf, tier, media, warnings, strikes, and activity.</div>`));const np=document.getElementById('newPostBtn');if(np)np.onclick=()=>openM('<h2>Create Thread</h2><div class="field"><label>Title</label><input></div><div class="field"><label>Message</label><textarea rows="6"></textarea></div><button class="btn btn-primary full">Post Thread</button>');
